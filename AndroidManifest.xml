<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jy.notewatermark"
    android:versionCode="3"
    android:versionName="2.1">

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="34" />

    <!-- The settings screen reaches the Notes app to trigger an export. -->
    <queries>
        <package android:name="com.coloros.note" />
    </queries>

    <application
        android:label="便签分享水印"
        android:hasCode="true">

        <activity
            android:name=".ConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".ConfigProvider"
            android:authorities="com.jy.notewatermark.settings"
            android:exported="true" />

        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="去掉/自定义 ColorOS 便签分享长图底部的水印，一键导出全部便签" />
        <meta-data android:name="xposedminversion" android:value="93" />
        <meta-data android:name="xposedscope" android:value="com.coloros.note" />
    </application>
</manifest>
