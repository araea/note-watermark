## 3.1.1 · 素笺

### 修正设置页顶栏图标过大

- 顶栏左上角此前直接使用应用图标。应用图标是 108dp 的自适应图标，在顶栏里按可用高度放大到约 71dp，比旁边的标题大一倍多。
- 改为随图标一起生成的 `ic_toolbar_logo`：取同一前景图层的字形，去掉自适应图标的外边距与背景色的角块，固定 28dp。
- 顶栏 logo 关闭 `setLogoAdjustViewBounds`，按自身尺寸显示，不再随顶栏高度放大，也不再被拉成非正方形。
- 字形与启动图标同源，改动 `artwork/icon.svg` 后仍由 `scripts/generate-icons.py` 一并重生成。

升级提示：沿用包名与签名，可直接覆盖升级。

验证：真机 instrumentation 全部通过，浅色、深色与大字号三组界面截图复核顶栏。
